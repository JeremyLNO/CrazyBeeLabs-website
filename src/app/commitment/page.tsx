import { CommitmentContent } from "@/components/commitment/CommitmentContent";

export const metadata = {
  title: "Our commitment",
  description:
    "Health shouldn't come with a price tag — especially women's health. Crazy Bee Labs builds certain health apps and gives them away for free.",
};

export default function CommitmentPage() {
  return <CommitmentContent />;
}
