import { OurApps } from "@/components/apps/OurApps";

export const metadata = {
  title: "Our apps",
  description:
    "Browse every Crazy Bee Labs app — filter by category and device. Mac and iPhone apps.",
};

export default function OurAppsPage() {
  return <OurApps />;
}
